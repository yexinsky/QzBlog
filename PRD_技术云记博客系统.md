# 技术云记博客系统 — 产品需求文档（PRD）

---

| 文档信息 | |
|---------|---|
| 产品名称 | 技术云记（个人博客系统，落地项目 QzBlog） |
| 版本 | v1.1 |
| 创建日期 | 2026-08-30 |
| 参考实现 | Halo 2.20.13 + Aurora 主题 1.0.3（竞品实测站点 http://8.138.194.104:8090 ） |
| 状态 | 依据竞品实测整理，并结合当前项目已有能力与方向修正 |

**修订记录（v1.1）：**

1. 竞品「瞬间」功能统一改称「动态」，与当前项目已有功能命名对齐，避免歧义
2. 主题管理、插件管理、应用市场移出本期范围（非当前项目方向），相关能力改为内置实现
3. 第三方对象存储（S3/OSS 等）暂不适配，仅保留「本地存储」占位，数据模型预留扩展字段
4. 管理后台页面路由统一以 `/console` 为前缀（当前项目现状为 `/admin`，需迁移）

---

## 1. 产品概述

### 1.1 产品定位

面向个人开发者的自托管博客系统，由「前台站点」与「管理后台」两部分组成。前台面向读者提供文章阅读、分类/标签浏览、动态查看、评论互动等能力；管理后台面向博主提供内容创作、媒体管理、用户与系统设置等一站式管理能力。

### 1.2 竞品基线（实测）

- 运行时：Halo 2.20.13（Java 21 / MySQL 8.1.0 / Linux）
- 已启用主题：Aurora 1.0.3
- 通过插件提供的能力：Markdown 编辑器（Willow）、对象存储（S3 协议）、瞬间（即本项目「动态」）、Sitemap、搜索组件、应用市场、RSS、评论组件
- 站点数据量级：文章 6 篇、分类 4 个、标签 4 个、自定义页面 4 个、附件 27 个、用户 1 人

> 说明：竞品上述「插件能力」在本项目中一律以**内置功能**实现，不引入插件机制；能力取舍见 3.1 本期范围外。

### 1.3 产品目标

- 让博主低摩擦地完成「写作 → 配置 → 发布」全流程，支持 Markdown 与版本管理
- 为读者提供清晰的内容组织（分类/标签/归档）与流畅的阅读、评论体验
- 提供完善的管理后台：内容、媒体、用户、系统设置、备份恢复一体化
- 保持架构克制：不实现主题/插件机制，预留存储等有限扩展点

---

## 2. 用户与角色

| 角色 | 说明 | 权限范围 |
|------|------|----------|
| 超级管理员 | 站点所有者 | 管理后台全部功能 |
| 注册用户 | 前台开放注册产生（可关闭） | 浏览、评论（受站点评论设置约束） |
| 访客 | 未登录的匿名访问者 | 浏览公开内容 |

> 竞品还内置了文章管理员/作者/投稿者等细分角色与角色自定义。当前项目为个人博客，仅博主一人管理，**本期仅保留超级管理员**，角色体系不做多级细分；数据模型预留 role 字段以便后续扩展。

---

## 3. 功能总览

| 模块 | 功能 | 优先级 |
|------|------|--------|
| 账号认证 | 登录、注册、忘记密码、保持会话 | P0 |
| 个人中心 | 资料编辑、消息通知 | P0 |
| 前台站点 | 首页文章流、分类/标签/归档、文章详情、动态页、关于页、搜索、评论、RSS | P0 |
| 仪表盘 | 站点统计、快捷访问、通知中心 | P0 |
| 文章管理 | 列表与筛选、分类/标签管理、回收站、Markdown 编辑器、文章设置、发布/下架 | P0 |
| 页面管理 | 自定义页面（关于、说说等单页）的创建与编辑 | P1 |
| 评论管理 | 评论审核、状态筛选、搜索 | P1 |
| 附件管理 | 上传、本地存储、分组管理 | P0 |
| 动态管理 | 图文短内容发布与管理（竞品称「瞬间」，即当前项目已有「动态」） | P0（已有） |
| 菜单管理 | 前台导航菜单的可视化管理 | P2 |
| 用户管理 | 管理员账号维护 | P1 |
| 系统设置 | 基本/文章/SEO/用户/评论/路由/代码注入/通知八组设置 | P0 |
| 系统运维 | 系统概览、备份与恢复、运行日志下载 | P1 |

### 3.1 本期范围外（不做清单）

| 不做项 | 原因 | 冲突规避说明 |
|--------|------|--------------|
| 主题管理 | 非当前项目方向，前台为自研固定主题 | 文章设置中的「自定义模板」依赖主题模板机制，本期不提供；系统设置中的「主题路由设置」更名「路由设置」，仅保留分类/标签/归档前缀配置，与主题无关 |
| 插件管理、应用市场 | 非当前项目方向，避免插件机制过度设计 | 竞品由插件提供的能力按下表改为内置实现或裁剪 |
| 第三方对象存储适配（S3/OSS/COS 等） | 暂无需求，仅保留本地存储 | 附件数据模型预留 `storage` 字段占位，后续接入第三方平台时无需迁移数据结构 |
| S3 关联工具 | 依赖对象存储适配，随之一并延后 | — |

**竞品插件能力 → 本项目实现方式对照：**

| 竞品插件 | 本项目实现方式 |
|----------|----------------|
| Willow Markdown 编辑器 | 内置 Markdown 编辑器（当前项目已实现，含 `/api/markdown/preview` 实时预览） |
| 瞬间 | 「动态」功能（当前项目已实现，前台 `/moments`，管理 `/console/moments`） |
| 搜索组件 | 内置全站搜索 |
| 评论组件 | 内置评论系统（当前项目已实现） |
| RSS | 内置 `/rss.xml`（当前项目已实现） |
| Sitemap | 内置 `/sitemap.xml`（P2，可后置） |
| 对象存储（S3 协议） | 本期仅本地存储，预留扩展点 |
| 应用市场 | 不做 |

---

## 4. 详细功能需求

### 4.1 账号与认证

#### 4.1.1 登录

**功能描述：** 管理后台的统一登录入口，未登录访问受保护页面时自动重定向至登录页（如 `/console` → `/console/login`）。

**需求详情：**

- 登录凭据：用户名（或邮箱）+ 密码；可选「保持登录会话」延长会话有效期
- 登录页提供「忘记密码」（邮箱重置流程）入口
- 竞品登录页的多语言切换（中/英/西/繁）本期不做，界面默认简体中文
- 登录成功后进入管理后台仪表盘
- 登录安全通知（向绑定邮箱发送登录提醒，含时间、IP、浏览器信息）作为 P1 增强，可后置

#### 4.1.2 注册

**功能描述：** 前台开放注册（默认关闭，可在系统设置中开启）。

**需求详情：**

- 开启后注册需验证邮箱（可配置开关）
- 当前项目为个人博客，默认关闭注册；注册用户仅用于评论身份

#### 4.1.3 密码找回

- 通过绑定邮箱发起重置请求，邮件中携带一次性重置链接
- 重置成功后旧会话失效

### 4.2 个人中心

**功能描述：** 博主账号的自助管理入口。

**需求详情：**

- **个人资料**：头像、显示名称、用户名（唯一）、电子邮箱、个人描述展示与编辑（当前项目已实现，位于后台「个人资料」页，路由 `/console/profile`）
- **消息通知**：站内通知中心，展示系统与业务通知（如评论提醒、登录提醒），支持已读管理
- 竞品的两步验证（2FA）、个人令牌（PAT）、登录设备管理为账号安全增强项，本期标记 **P2 暂缓**，不阻塞主流程
- 竞品将个人中心独立于 `/uc` 前缀；当前项目博主资料在管理后台内，本期沿用该方案，不单设 `/uc`

### 4.3 前台站点

#### 4.3.1 首页

- 顶部导航：本期为固定渲染（与当前项目一致：首页、文章、分类、标签、动态、关于等）；可视化管理见 4.10 菜单管理（P2）
- 文章列表：卡片式展示（分类、标题、摘要、作者信息、发布日期、标签）
- 分类快捷筛选 Tab 与列表分页
- 侧栏：作者卡片（头像、签名、社交链接、文章数、分类数、访问量、建站天数）、标签目录（含文章计数）、最近评论
- 页脚：版权信息、ICP 备案与公网安备链接（合规要求）、浏览次数与建站日期
- 浮动工具：返回顶部、打开搜索

#### 4.3.2 文章阅读

- 文章详情页：标题、正文 Markdown 渲染（代码高亮、表格、图片）、分类与标签展示、发布信息
- 目录（TOC）、作者信息、评论区
- 阅读量计入站点统计（列表与管理后台可见）

#### 4.3.3 聚合页面

- 标签页 `/tags`：全部标签与标签下文章
- 分类页 `/categories/{slug}`：分类下文章列表
- 归档页：按时间归档浏览（路由前缀可在路由设置中配置）
- 动态页 `/moments`：动态时间线展示（与当前项目路由一致）
- 关于页 `/about`：自定义页面渲染

#### 4.3.4 站点级能力

- **搜索**：内置全站关键词检索
- **RSS**：`/rss.xml`（当前项目已实现）
- **Sitemap**：自动生成站点地图，供搜索引擎抓取（P2）
- **搜索引擎刷新**：管理后台提供手动通知搜索引擎更新的入口（P2，可后置）

### 4.4 管理后台 — 仪表盘（/console）

**功能描述：** 登录后的默认落地页，聚合站点状态与高频操作。

**需求详情：**

- 统计卡片：文章数、评论数、浏览量、动态数（竞品为用户数；本项目单用户，改为动态数更有价值）
- 快捷访问入口：查看站点、创建文章、创建页面、附件上传、个人资料
- 通知面板：展示最近通知，支持「查看全部」
- 空状态与加载状态设计

### 4.5 管理后台 — 文章管理（/console/posts）

#### 4.5.1 文章列表

- 顶部操作：分类管理、标签管理、回收站、新建文章
- 批量选择（复选框）+ 批量操作
- 关键词搜索；筛选维度：状态（全部/已发布/草稿）、分类、标签；排序方式可切换
- 列表项信息：标题（点击进入编辑器）、分类、标签、访问量、评论数、状态徽标、发布/更新时间
- 分页：每页 20/30/50/100 条可切换，页码导航

#### 4.5.2 分类管理

- 新建分类；展示：名称、访问路径（如 `/categories/yu-yan`）、文章计数
- 支持编辑（名称、别名、描述）、删除、排序；删除前校验分类下是否有文章

#### 4.5.3 标签管理

- 新建标签；展示：名称、路径（如 `/tags/biji`）、文章计数
- 支持搜索、编辑、删除

#### 4.5.4 回收站

- 已删除文章列表，展示标题、删除时间
- 支持恢复与彻底删除；支持搜索
- 当前项目已有草稿机制（`/api/posts/[slug]/draft`），回收站为新增能力

#### 4.5.5 文章编辑器（/console/posts/new、/console/posts/{slug}/edit）

- **编辑器**：Markdown 编辑器，内置实现（当前项目已具备编辑/编辑草稿/Markdown 预览能力），功能对齐竞品：
  - 工具栏快捷操作：加粗、斜体、标题、引用、列表、代码、表格、链接、图片等
  - 实时预览（当前项目已有 `/api/markdown/preview`）
  - 自动保存草稿，防止内容丢失
- **顶部操作**：预览、保存（草稿）、设置、发布
- **版本历史**：内容快照记录，可查看与回滚（P2，可后置）
- **图片插入**：支持从附件库选择或上传（本地存储）

#### 4.5.6 文章设置（编辑器侧边抽屉）

- **常规设置**：
  - 标题（必填）
  - 别名/slug（必填，用于生成固定链接，默认按标题转写生成，可改写；当前项目以 slug 为文章路由键，需保持唯一性校验）
  - 分类目录（选择）
  - 标签（多选，可新建）
  - 自动生成摘要（开关；关闭后可手动填写摘要）
- **高级设置**：
  - 允许评论（开关）
  - 是否置顶（开关）
  - 可见性：公开 / 私有（私有仅登录博主可见）
  - 发表时间（支持定时发布）
  - 封面图（从附件选择或 URL）
  - 元数据（自定义 key-value，供 SEO 使用）
- **操作**：保存、关闭、取消发布（已发布文章可下架回草稿）

#### 4.5.7 发布流程

- 草稿 → 发布：校验标题等必填项后发布，前台立即可见
- 定时发布：设置未来发表时间，到点自动发布（P1）
- 已发布文章支持：编辑更新、取消发布、移入回收站

### 4.6 管理后台 — 页面管理（/console/pages）

**功能描述：** 管理独立于文章流的「自定义页面」（如：关于、友链等单页）。

**需求详情：**

- 页面列表：标题（点击进入编辑器）、访问量、状态、时间；支持新建与批量操作
- 页面编辑器与文章编辑器一致（Markdown + 设置抽屉），设置项含别名、可见性、允许评论等
- 前台固定路由（如 `/about`）可绑定自定义页面内容

### 4.7 管理后台 — 评论管理（/console/comments）

- 筛选维度：状态（全部/待审核/已通过/垃圾评论）、关键词搜索、排序
- 列表展示评论内容、评论对象（文章/动态）、评论者信息、时间
- 操作：通过审核、拒绝/标记垃圾、回复、删除；支持批量操作（当前项目已有评论管理基础，审核流为增强）
- 空状态设计：无评论时展示引导文案
- 站点级评论策略由系统设置（4.11.4）控制：启用评论、新评论审核、仅允许注册用户评论

### 4.8 管理后台 — 附件管理（/console/attachments）

- 附件库：文件名、类型（MIME）、大小、上传者、上传时间；列表/网格视图，多选批量操作
- 筛选：按分组（全部/未分组/自定义分组）、按类型、关键词搜索
- 分页规格：每页 60/120/200 条
- **上传**：支持多文件、拖拽上传，上传至指定分组（当前项目已有 `/api/upload`）
- **存储**：
  - 本期仅实现**本地存储**（服务器磁盘），提供存储目录可配置
  - 数据模型预留 `storage` 字段与策略接口占位，后续可扩展第三方对象存储（S3/OSS/COS 等），本期不实现、不做任何第三方 SDK 集成
- **分组管理**：新建/重命名/删除分组，附件可在分组间移动

### 4.9 管理后台 — 动态管理（/console/moments）

**功能描述：** 轻量级碎片化内容（当前项目称「动态」，竞品 Halo 称「瞬间」，两者为同一功能；本文档统一用「动态」）的发布与管理。

**需求详情：**

- 发布框：Markdown 短内容输入，支持图片插入
- 列表：昵称、相对时间、内容、图片；支持编辑与删除（当前项目已实现基础版 `/api/moments`）
- 筛选：按状态（全部/公开/私密）、按日期范围（增强项）
- 「跳转到前台」直达前台动态页 `/moments`（与当前项目路由一致）
- 动态支持评论（前台经由评论系统）

> 措辞约定：全文及界面文案一律使用「动态」，不出现「瞬间」，避免与竞品术语混淆。

### 4.10 管理后台 — 菜单管理（/console/menus，P2）

- 前台导航当前为固定渲染；本期可选做菜单可视化管理
- 菜单项可指向：自定义页面 / 文章 / 分类 / 标签 / 外部链接；支持显示文本、排序与层级嵌套
- 若不实现，前台导航以代码配置方式维护即可，不阻塞其他功能

### 4.11 管理后台 — 系统管理

#### 4.11.1 用户管理（/console/users）

- 当前项目为单博主，本期仅提供管理员账号维护：查看账号信息、修改密码
- 新建子账号、角色分配等能力延后（见第 2 节角色说明）

#### 4.11.2 系统概览（/console/overview）

- **基本信息**：外部访问地址、启动时间、一键复制
- **环境信息**：应用版本、构建时间、Node/数据库版本、操作系统、系统时区
- **运行日志**：支持下载运行日志文件

#### 4.11.3 备份与恢复（/console/backup）

- 创建备份：打包站点数据（数据库 + 本地附件）生成备份文件，展示备份进度与历史备份列表（含下载、删除）
- 恢复：上传备份包恢复整站数据（危险操作需二次确认）

#### 4.11.4 系统设置（/console/settings）

八组设置，均独立保存：

1. **基本设置**：站点标题（必填）、站点副标题、Logo、Favicon
2. **文章设置**：文章列表/归档页/分类页/标签页显示条数、默认分类
3. **SEO 设置**：屏蔽搜索引擎（开关）、站点关键词、站点描述
4. **用户设置**：开放注册（开关，默认关闭）、注册需验证邮箱（开关）
5. **评论设置**：启用评论、新评论审核、仅允许注册用户评论
6. **路由设置**：分类页/标签页/归档页路由前缀自定义（默认 categories / tags / archives）。注意：修改前缀需做旧链接 301 兼容，且与当前项目现有路由对齐
7. **代码注入**：全局 Head、内容块 Head/Footer 三处自定义代码注入（统计脚本、字体等）
8. **通知设置**：邮件通知器（SMTP 主机、端口、用户名、密码、发信地址、显示名称），提供「测试邮箱」验证；作为登录提醒、评论通知、密码重置等邮件的发送通道

### 4.12 全局交互

- **全局搜索（Ctrl+K）**：管理后台侧边栏提供快捷搜索入口，可快速跳转各管理页面
- **通知体系**：站内通知 + 邮件通知双通道；触发场景包括登录提醒、评论、系统公告
- **响应式**：管理后台与前台适配桌面与移动端

---

## 5. 非功能需求

### 5.1 安全

- 会话管理：登录态校验，受保护路由未登录重定向；可选「保持登录会话」
- 基于角色的权限控制（RBAC）框架预留，本期仅超级管理员单一角色
- 评论审核机制防垃圾内容；SEO 设置支持一键屏蔽搜索引擎
- 敏感配置（SMTP 密码等）落库需加密，界面不明文回显
- 后台 API 鉴权：`/api/admin/**` 统一鉴权中间件，路由前缀迁移时需同步覆盖 `/console` 页面路由

### 5.2 性能与可靠性

- 附件本地存储；数据模型预留第三方存储扩展点（本期不实现）
- 数据量增长时分页加载（列表默认 20 条/页）
- 备份恢复机制保障数据可迁移、可回滚

### 5.3 可扩展性

- 不实现主题/插件机制（避免过度设计），前台为自研固定主题
- 预留扩展点：附件存储策略（本地 → 第三方对象存储）、角色体系（单管理员 → 多角色）
- 提供开放 API 供第三方集成（P2）

### 5.4 SEO 与合规

- 语义化路由（分类/标签/归档前缀可配置）
- RSS 输出（已实现）、Sitemap（P2）
- 页脚 ICP 备案号与公网安备链接展示（合规要求）

---

## 6. 页面结构与路由清单

### 6.1 路由约定（重要）

**管理后台所有页面路由统一以 `/console` 为前缀。** 当前项目现状为 `/admin` 前缀，需按下方映射迁移：

| 现状路由（/admin） | 目标路由（/console） | 说明 |
|--------------------|----------------------|------|
| `/admin` | `/console` | 仪表盘（登录后落地页） |
| `/admin/login` | `/console/login` | 后台登录页 |
| `/admin/posts` | `/console/posts` | 文章列表 |
| `/admin/posts/new` | `/console/posts/new` | 新建文章 |
| `/admin/posts/[slug]/edit` | `/console/posts/[slug]/edit` | 编辑文章 |
| `/admin/moments` | `/console/moments` | 动态管理 |
| `/admin/comments` | `/console/comments` | 评论管理 |
| `/admin/profile` | `/console/profile` | 个人资料 |

迁移注意事项（冲突点）：

- 旧地址 `/admin/**` 做 301 重定向到 `/console/**`，避免收藏夹/搜索引擎失效
- 鉴权中间件（session 守卫）需同时识别新旧前缀，迁移完成后移除旧逻辑
- 后端 API 路由 `/api/admin/**` **保持不变**（仅页面路由前缀调整），避免连带改动与前后端联调成本
- 前台路由不受影响（`/`、`/posts`、`/moments`、`/categories`、`/tags`、`/about` 等维持现状）

### 6.2 前台路由

| 路由 | 页面 | 现状 |
|------|------|------|
| `/` | 首页（文章流 + 侧栏） | 已有 |
| `/posts`、`/posts/{slug}` | 文章列表/详情 | 已有 |
| `/categories`、`/categories/{slug}` | 分类页 | 已有 |
| `/tags`、`/tags/{slug}` | 标签页 | 已有 |
| `/moments`、`/moments/{id}` | 动态页 | 已有 |
| `/about` | 关于（自定义页面） | 已有 |
| `/login`、`/signup`、`/password-reset` | 登录/注册/找回密码 | 部分已有 |
| `/rss.xml` | RSS 订阅 | 已有 |
| `/sitemap.xml` | 站点地图 | 待实现（P2） |

### 6.3 管理后台路由（目标态，/console 前缀）

| 路由 | 模块 | 现状 |
|------|------|------|
| `/console` | 仪表盘 | 已有（/admin） |
| `/console/login` | 后台登录 | 已有（/admin/login） |
| `/console/posts`、`/console/posts/new`、`/console/posts/[slug]/edit` | 文章管理 | 已有（/admin/posts） |
| `/console/posts/categories`、`/console/posts/tags`、`/console/posts/recycle-bin` | 分类/标签/回收站 | 分类/标签已有，回收站待实现 |
| `/console/pages`、`/console/pages/editor` | 页面管理 | 待实现 |
| `/console/comments` | 评论管理 | 已有（/admin/comments） |
| `/console/attachments` | 附件管理 | 待实现（上传 API 已有） |
| `/console/moments` | 动态管理 | 已有（/admin/moments） |
| `/console/menus` | 菜单管理 | P2，可选 |
| `/console/users` | 用户管理（单管理员维护） | 待实现 |
| `/console/settings` | 系统设置 | 待实现 |
| `/console/overview` | 系统概览 | 待实现 |
| `/console/backup` | 备份与恢复 | 待实现 |

---

## 7. 数据模型（核心实体）

| 实体 | 关键字段 | 说明 |
|------|----------|------|
| User | username、displayName、email、password、avatar、bio、role | 站点用户；本期仅超级管理员，role 字段预留 |
| Post | title、slug、content、category、tags[]、cover、excerpt、visible(公开/私有)、pinned、allowComment、publishTime、status(草稿/已发布/回收站) | 文章主体；slug 为路由键需唯一 |
| Category | name、slug、description、postCount | 分类 |
| Tag | name、slug、postCount | 标签 |
| SinglePage | title、slug、content、visible | 自定义页面 |
| Comment | content、owner、target(文章/动态)、status(待审/通过/垃圾)、parentId | 评论（支持嵌套回复） |
| Attachment | filename、mimeType、size、groupId、**storage（预留，当前固定 local）**、uploader | 附件；storage 字段为第三方存储占位 |
| AttachmentGroup | displayName | 附件分组 |
| Moment | content、medium[]、visible、createTime | **动态**（竞品称「瞬间」，本项目统一叫「动态」） |
| Menu / MenuItem | menuName、items[]{text、targetType、href、children} | 菜单集与菜单项（P2） |
| Setting | group、key、value | 系统设置项（按八组组织） |
| Backup | filename、phase、size、createTime | 备份记录 |
| Notification | recipient、title、content、readStatus | 站内通知 |

> 主题（Theme）与插件（Plugin）实体本期不建模（不做主题/插件机制）；后续接入第三方存储时基于 Attachment.storage 字段扩展，无需迁移数据。

---

## 8. 验收要点（关键路径）

1. 未登录访问 `/console` 跳转 `/console/login`；登录后落至仪表盘
2. 旧地址 `/admin/**` 301 重定向至 `/console/**`，后台 API `/api/admin/**` 不受影响
3. 新建文章 → 编辑 Markdown → 设置分类/标签/别名 → 发布 → 前台首页与详情页可见、计数正确
4. 已发布文章「取消发布」回到草稿；删除进入回收站并可恢复
5. 上传附件至自定义分组（本地存储），文章编辑器可引用；附件记录 `storage=local`
6. 发表图文动态，前台 `/moments` 时间线可见并可评论；管理后台可编辑/删除
7. 开启「新评论审核」后新评论默认待审，后台审核通过后前台可见
8. 关闭「开放注册」后注册入口失效
9. 创建备份并可下载；恢复备份后站点数据还原
10. SMTP「测试邮箱」通过后，密码重置/评论通知等邮件可达
